#include <stdio.h>
#include <stdlib.h>
#include <string.h>
//180202134
//bahadır aşık

int main()
{
    char veri[100];
    printf("Ulke isimlerini ve Oranları girin");
    gets(veri);
    int temp=0;
    int matris[10][20];
    int a=0;
    int b=0;
    printf("Sıralama Olmadan:\n");
    for(int i=temp;veri[i]!='\0';i++){
        printf("%c",veri[i]);
        matris[a][b]=veri[i];
        b++;
        if(isdigit(veri[i]))
        {
            if(isalpha(veri[i+1])){
                printf("\n");
                a++;
                b=0;
            }
        }
        if(isalpha(veri[i]))
        {
            if(isdigit(veri[i+1])){
                printf(":");
            }
        }
    }
    int s[5];
    for(int i=0;i<5;i++){ s[i]=0;}
    //printf("%d",a+1);
    printf("Siralanmıs Hali\n");
    for(int i=0;i<a+1;i++){
        if(matris[i][s[i]-1]<matris[i+1][s[i]-1]){
        
        }
    }
    return 0;
}
